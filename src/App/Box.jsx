import styled from 'styled-components';
import {
  color,
  space,
  layout,
  shadow,
  typography,
  flexbox,
  border
} from 'styled-system';

export const Box = styled('div')(
  color,
  space,
  layout,
  shadow,
  typography,
  flexbox, border
);
